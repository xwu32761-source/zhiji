# 「知己」APP —— 完整初始开发维度文档（V1.0）
文档定位：此文档将作为 Claude Code 学习整个项目背景、产品逻辑、技术架构、交互规范的核心上下文。请基于此文档生成完整可运行的 Web 端 MVP 代码。

当前阶段：Web 端 MVP（响应式，适配 PC/平板/手机）。
后续规划：小程序（裂变拉新）→ APP（高粘性交互，收入变现）。

## 一、战略与概念维度（顶层建筑）
### 1.1 北极星指标（North Star）
核心指标：“月度深度洞察报告生成数”（即 Tab4 报告每月被成功唤醒的次数）。

为什么选它：它同时覆盖了：① 用户数据采集量（没有足够数据无法生成）；② 用户情感峰值体验（生成报告是最高价值时刻）；③ 潜在付费转化点（每月超过3次后需付费）。

辅助指标：日均“定格”记录数（反映习惯养成） + 周报分享率（反映社交传播）。

### 1.2 价值主张切片（极端痛点定位）
3秒痛点：打开 APP → 看到一句话精准描述了你此刻的情绪状态。

核心文案（首页/市场宣传）：

“一本关于自己的说明书就是你最好的心理医生。”

“记录即疗愈。AI 帮你发现你自己都忽略的生存模式。”

对用户的价值：从“我不知道我怎么了”到“原来我是这样的人，且我可以这样做”。

### 1.3 MVP 边界与冗余（坚决不做 vs 预留接口）
坚决不做（MVP）	但架构预留接口（为未来）
社区/广场（UGC内容流）	social 命名空间，预留 Feed 表结构
心理咨询师真人入驻/预约	therapist 模块，预留 consultant_pool 表
复杂的数据导出（PDF/Excel）	在 Tab4 底部预留“导出”按钮，但显示“会员专享”
微信/支付宝支付完整闭环	 在“刷新报告”次数用尽时，仅展示升级入口，不实现支付
推送通知系统（Push）	在用户设置页保留 notification_settings 字段
AI 主动每日问候（每日卡片）	在 Tab2 保留 daily_card 接口占位

### 1.4 多端生态定位
| 端 | 职责 | 交互重心 |
|Web（当前）|MVP 验证 + SEO 内容引流。完整功能体验，响应式适配。|键盘输入 + 鼠标点击，长文本沉浸阅读（报告）。|
|小程序     |社交裂变拉新。核心功能仅保留“定格”+“周报分享”。	|快速记录，分享卡片（人格徽章），拉起 APP/Web。|
|APP	   |高粘性付费用户承载。全部功能 + 离线缓存 + 推送提醒。|手势交互，震动反馈，深度沉浸，桌面小组件（未来）。|

## 二、用户心理与行为维度（底层逻辑）
### 2.1 用户心智模型
用户潜意识认知：把 APP 当成 “一个住在手机里的、极度懂我的、非批判性的观察者/记录员”。

文案调性：温柔、笃定、不模棱两可、有文学感。禁止使用“您”、“可能”、“或许”、“应该”等疏离或模糊词汇。统一使用“你”。

### 2.2 上瘾模型（Hook）闭环路径设计
触发（内因）：用户感到情绪波动时（焦虑、开心、孤独）→ 行动（外因）：打开 APP 进行“定格”记录（15秒内完成）→ 多变的酬赏：每次“定格”后 AI 生成一个不重复的“记忆钩子”/随机金句/情绪色块（每次不一样，制造惊喜）→ 投入：记录自动存入日记，积累数据解锁 Tab4 报告。用户在投入时间后，因损失厌恶不愿放弃。

### 2.3 损失厌恶设计
连续记录徽章：连续记录 3 天获得“观察者”徽章；7 天“觉醒者”；14 天“溯源者”；30 天“半神”。

进度可视化：首页 Tab1 的 10 大支柱进度环、Tab4 的“破茧进度条”，让用户因“不想让进度归零”而持续使用。

### 2.4 从众效应锚点
埋点位置：

Tab2 入口页底部：“已有 12,847 人今天记录了自己的瞬间。”

Tab1 某个支柱详情页：“87% 的用户在这一维度选择了‘夜猫子’标签。”

Tab4 报告底部：“你的报告已为 4,231 份人生样本增添独特性。”

## 三、全链路动线与场景流（体验骨架）
### 3.1 新用户黄金动线（30秒 / 3步）
下载/打开 Web → 看到价值主张页（3秒）。

点击”开始探索” → 自动进入 Tab1 主页 → 看到 12 大支柱 + 引导浮层”去记录此刻”（5秒）。

点击引导浮层跳转 Tab2 → 完成首次”定格”记录（15秒）。

✅ 完成首次核心动作。进度环显示 0/12，Tab4 显示”还差 4 条记录唤醒手册”。

### 3.2 核心任务闭环动线（定格→存入→回流）
Tab2 选择分类 → 选核心词/调强度/填来源 → 点击“定格” → Toast 反馈 → 后台存入 diary_entries → 实时更新 total_entries_count → 触发进度检查 → 若达标则点亮 Tab4。

闭环无断点：每一步都在同一页面完成，无跳转。

### 3.3 流失挽回动线
切后台/关闭浏览器前：若用户正在 Tab2 输入框停留超过 20 秒且未提交，触发 beforeunload 提示：“你的思绪即将溜走，要把它定格下来吗？”

Tab4 报告生成等待页：若用户点击“返回”，弹出挽留：“报告即将完成，再等 3 秒就能看到‘另一个你’。”

### 3.4 异常状态动线（全场景覆盖）
弱网/断网：所有接口请求先读缓存（IndexedDB / localStorage），展示缓存数据，同时顶部显示黄条横幅：“网络不稳定，数据将本地暂存，恢复后同步”。

服务器 500：显示一个极简错误页，非白屏，带“重试”按钮 + “去社群反馈”入口。

空状态：Tab3 日记无记录时显示：“这里还很安静。去定格一个瞬间，让时光泛起涟漪吧。”

权限拒绝（如 localStorage 被禁用）：提示：“为了保存你的人生印记，请允许使用本地存储。”

### 3.5 跨端跳转动线（Web→小程序/APP 预留）
Web 端在“分享人格徽章”时，生成的图片自带二维码：扫二维码 → 小程序（后续）→ 若检测到未安装 APP，引导至应用商店。

状态保持：无论从哪个端进入，用户需登录（手机号/微信）以保持同一身份。

### 3.6 情绪曲线设计（关键节点）
加载等待期（低谷）：在 Tab4 报告生成加载页，展示动态书写的墨迹动画 + 底部轮流播放 5 条“预热金句”：“正在整理你的昨日碎片……”、“寻找你未言说的剧本……”、“编织只属于你的暗涌图谱……”——让等待变期待。

成功保存（高峰）：每次“定格”成功，Toast 文案随机变化（避免重复导致倦怠），如“✨ 已捕获”、“🌊 汇入长河”、“📌 钉在时光墙上”。

## 四、信息架构与页面层级（骨架）
### 4.1 二维导航策略（Web 端适配）
底部固定 Tab（4个），不超过5个：

📖 我的前传（Tab1）
⚡ 定格此刻（Tab2）
📅 时光日记（Tab3）
📄 说明书（Tab4）
顶部二级切换：仅 Tab3（日记）需顶部左右滑动切换月份（← 2026年7月 →），其余 Tab 无二级导航。

手势侧滑：Web 端仅支持鼠标点击，不强制手势；但为 APP 预留 touch 事件监听，左右滑切换月份。

### 4.2 页面层级深度（任何功能 ≤ 3 级）
一级：底部 Tab 切换。

二级：点击 Tab1 的支柱卡片进入详情页；点击 Tab3 日记卡片展开详情。

三级：点击日记展开后的“完整报告”半屏弹窗（Modal）。绝无四级。

### 4.3 模态与平层决策
场景	使用组件	原因
删除记录确认	模态弹窗（Modal）	需用户明确决策，防止误触。
定格成功	Toast（轻提示）	告知型，不打断操作。
进度达标解锁报告	横幅（Banner）	半引导型，在页面顶部滑动出现，不强制点击。
Tab1 表单校验错误	内联提示（Inline）	直接在字段下方显示，不弹窗。
报告生成完成	全屏过渡（Fullscreen Modal）	仪式感强，用户从加载页平滑进入报告页。

## 五、UI视觉与设计系统（皮囊与肌肉）
### 5.1 设计令牌（Design Tokens）—— 原子化定义
颜色（Colors）
|Token|	值	|用途
|--color-primary|	#5B6ABF（静谧紫）|	主按钮、高亮文字、进度环|
|--color-primary-light|	#E8ECF9	|卡片背景、选中态|
|--color-secondary|	#F5A3A3（温柔粉）|	负面情绪高亮、标记|
|--color-success|	#8BCB9E（生机绿）|	正向情绪、完成状态|
|--color-text-primary|	#1A1A2E|	正文|
|--color-text-secondary|	#6B6B80	|辅助说明、占位符|
|--color-bg|	#F9F8FC（极暖白）	|全局背景|
|--color-card-bg|	#FFFFFF	|卡片背景|
|--color-border|	#E2E0ED|	分割线、边框|
字体（Typography）
|Token	| 值	|用途|
|--font-family-base	|-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "PingFang SC", sans-serif|	正文|
|--font-family-serif	|"Georgia", "Songti SC", serif	|报告标题、AI密信|
|--font-size-xs	|12px	|标签、辅助信息|
|--font-size-sm	|14px	|正文、按钮文字|
|--font-size-md	|16px	|卡片标题、输入框|
|--font-size-lg|	20px|	页面标题|
|--font-size-xl	|24px	|Tab4 封面人格称呼|
|--font-size-2xl	|32px|	进度环中央数字|
|--line-height-relaxed|	1.8	|报告正文（增加阅读舒适度）|
间距与圆角（Spacing & Radius）
|Token	|值|
|--spacing-xs	|4px|
|--spacing-sm	|8px|
|--spacing-md	|16px|
|--spacing-lg	|24px|
|--spacing-xl	|32px|
|--radius-sm	|8px|
|--radius-md	|12px|
|--radius-lg	|20px|
|--radius-full	|9999px（胶囊按钮）|
阴影（Shadows）
|Token|	值	|用途|
|--shadow-sm	|0 2px 8px rgba(0,0,0,0.06)|	卡片默认|
|--shadow-md	|0 4px 16px rgba(91,106,191,0.12)|	悬浮按钮、模态|
|--shadow-lg	|0 8px 32px rgba(0,0,0,0.10)	|报告卡片、弹窗|
### 5.2 无障碍设计（A11y）
色彩对比度：所有正文与背景对比度 ≥ 4.5:1（符合 WCAG 2.1 AA 级）。

检查点：--color-text-primary (#1A1A2E) 在 --color-bg (#F9F8FC) 上对比度约 15:1 ✅。

主按钮文字（白色）在 --color-primary (#5B6ABF) 上对比度约 6.5:1 ✅。

字体缩放：所有字体使用 rem 单位，支持浏览器缩放。

旁白标签：所有交互元素必须有 aria-label，如按钮“定格此刻”标为 aria-label="记录当前情绪瞬间"。

### 5.3 手势与热区
点击热区：所有可点击元素（按钮、卡片、标签）最小 44px × 44px（符合 Fitts 定律）。

防误触：Tab3 左右滑动切换月份的区域仅限顶部导航栏区域（高度 56px），正文区域滑动不触发切换（避免误操作）。

### 5.4 加载态与占位图
骨架屏（Skeleton Screen）：

Tab1 支柱卡片列表加载时，显示 6 个灰块（模拟卡片），宽度渐变脉冲动画。

Tab3 日记列表加载时，显示 5 个灰条矩形（模拟日记卡片）。

禁止使用转菊花（Spinner）作为主要加载反馈，仅在接口请求超时（>5s）时兜底显示。

## 六、技术架构与接口（API）维度（血液循环）
### 6.1 接口范式
Web 端：采用 RESTful API（简单直观，一人团队开发效率高）。

预留 GraphQL 接入点：在网关层保留 /graphql 路由，为未来 APP 端做增量字段请求做准备。

### 6.2 接口版本管理
所有 API 路径带版本号：/api/v1/xxx。

版本升级策略：老版本接口至少维护 6 个月，并在响应头中带 Deprecation: true 警告。

### 6.3 接口幂等性
必须幂等的接口：

POST /api/v1/entries（定格记录）：前端传 idempotency_key（UUID），后端存入 Redis 缓存 24 小时，重复请求返回同一结果。

POST /api/v1/manual/generate（生成报告）：同上。

非幂等接口：GET 请求天然幂等，无需处理。

### 6.4 超时与重试策略
接口类型	超时时间	重试次数	重试间隔（指数退避）
定格记录（轻量 AI）	3s	2 次	1s, 2s
报告生成（重量 AI）	30s	1 次	3s
图表数据聚合（Tab3）	3s	2 次	1s, 2s
静态资源（图片/CSS）	10s	3 次	1s, 2s, 4s
### 6.5 缓存策略（Cache-Aside）
前端缓存：IndexedDB（存储 user_profile、diary_entries 最近 30 天数据），离线可用。

后端缓存：Redis（存储用户会话、热点数据如 Tab1 支柱标签库、Tab3 月度情绪聚合结果）。

CDN：所有静态资源（JS/CSS/字体）走 CDN，图片使用 WebP 格式。

缓存更新策略：先返回缓存数据（秒开），然后异步请求接口更新缓存（stale-while-revalidate）。

### 6.6 AI 服务调用策略
统一 AI 网关：封装为内部服务 ai-service，支持切换模型（Claude / GPT-4 / 本地小模型）。

降级方案：若 AI 服务超时或报错，返回预置的“备胎回复模板”（如“我正在思考，稍后给你更完整的回响……”），并在后台记录失败任务，异步重试。

## 七、数据表与数据库维度（骨骼内脏）
### 7.1 数据库选型
主库：PostgreSQL 15（支持 JSONB，灵活存储用户画像）。

缓存库：Redis 7（会话、幂等键、热点缓存）。

对象存储：AWS S3 / 阿里云 OSS（存储用户上传图片、分享卡片图片）。

### 7.2 核心表结构设计（含分库分表预留）
表1：users（用户表）
sql
CREATE TABLE users (
  id BIGINT PRIMARY KEY, -- 雪花算法生成
  phone VARCHAR(20) UNIQUE,
  wechat_openid VARCHAR(64) UNIQUE,
  nickname VARCHAR(50) DEFAULT '探索者',
  avatar_url TEXT,
  timezone VARCHAR(50) DEFAULT 'Asia/Shanghai',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW(),
  deleted_at TIMESTAMP -- 软删除
);
-- 分片键：id（取模）
-- 索引：phone, wechat_openid, created_at
表2：user_profiles（Tab1 数据，JSONB 存储）
sql
CREATE TABLE user_profiles (
  id BIGINT PRIMARY KEY,
  user_id BIGINT NOT NULL,
  basic_answers JSONB NOT NULL, -- {"p1": {"q1": "A", "q2": 7}, "p2": {...}}
  deep_tags JSONB, -- ["tag_id_101", "tag_id_205"]
  pillar_completed INTEGER[] DEFAULT '{}', -- [1,2,3]
  first_profile_at TIMESTAMP,
  last_seal_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
-- 分片键：user_id
-- 索引：user_id (唯一)
表3：diary_entries（Tab2 所有记录）
sql
CREATE TABLE diary_entries (
  id BIGINT PRIMARY KEY,
  user_id BIGINT NOT NULL,
  entry_type VARCHAR(20) NOT NULL, -- 'emotion' / 'behavior' / 'intake' / 'narrative'
  core_tag VARCHAR(50), -- '愤怒' / '跑步' / '冰美式'
  intensity SMALLINT, -- 1-5 (情绪/行为强度)
  source VARCHAR(20), -- '他人' / '自身' / '外物'
  note TEXT, -- 用户自定义短句
  ai_hook VARCHAR(100), -- AI生成的记忆钩子
  linked_pillars INTEGER[], -- [2,4]
  full_report TEXT, -- 入口一叙事完整报告（仅 narrative 类型有值）
  score SMALLINT, -- 情绪得分 -5 ~ +5
  entry_date DATE NOT NULL, -- 记录日期（用于 Tab3 按天聚合）
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
-- 分片键：user_id
-- 索引：联合索引 (user_id, entry_date DESC), (user_id, created_at DESC)
-- 冷热分离策略：entry_date > NOW() - INTERVAL '3 months' 热数据，其余归档到 history 表
表4：life_manual（Tab4 报告缓存）
sql
CREATE TABLE life_manual (
  id BIGINT PRIMARY KEY,
  user_id BIGINT NOT NULL,
  version INTEGER NOT NULL, -- 版本号，每次刷新+1
  cover_title VARCHAR(50), -- 封面人格称呼
  full_text TEXT, -- 完整 Markdown 报告
  generated_at TIMESTAMP DEFAULT NOW(),
  is_current BOOLEAN DEFAULT TRUE -- 当前生效版本
);
-- 分片键：user_id
-- 索引：user_id, (user_id, is_current)
表5：user_quotas（免费刷新次数等）
sql
CREATE TABLE user_quotas (
  user_id BIGINT PRIMARY KEY,
  refresh_quota INTEGER DEFAULT 3, -- 本月剩余刷新次数
  quota_reset_month DATE DEFAULT date_trunc('month', NOW()), -- 上次重置月份
  total_entries_count INTEGER DEFAULT 0, -- 汇总字段（避免 COUNT 全表）
  continuous_days INTEGER DEFAULT 0, -- 连续记录天数
  last_entry_date DATE,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
-- 分片键：user_id
### 7.3 主键生成策略（雪花算法）
Worker ID：固定为 1（单机部署），通过时间戳 + 序列号保证全局唯一。

Go 实现：使用 sonyflake 或 twitter/snowflake 库。

Node.js 实现：使用 node-snowflake。

### 7.4 软删除与数据归档
软删除：所有表带 deleted_at，查询时默认加 WHERE deleted_at IS NULL。

数据归档：每月 1 日凌晨，将 entry_date < NOW() - INTERVAL '3 months' 的日记记录迁移至 diary_entries_archive 分区表。

## 八、数据埋点与增长分析维度（神经系统）
### 8.1 埋点工具选型
Web：自研轻量埋点 SDK（封装 window.gtag 或 navigator.sendBeacon），上报至简单数据收集服务（或直接使用 Umami / Plausible 自托管）。

核心事件：

page_view（页面浏览）

entry_created（定格/叙事记录）

manual_generated（报告生成）

manual_refreshed（报告刷新）

share_card_created（分享卡片生成）

profile_completed_pillar（完成一个支柱）

### 8.2 漏斗分析预埋（关键转化路径）
| 步骤|	事件名|	目标转化率|
|1. 访问首页	|page_view_home	|100%|
|2. 完成 3 题建档|	profile_3q_completed	|≥ 60%|
|3. 首次“定格”|	first_entry_created	|≥ 40%|
|4. 累计 5 条记录	|entries_5_reached	|≥ 25%|
|5. 生成初版报告	|first_manual_generated|≥ 15%|
|6. 分享卡片|	share_card_created|	≥ 8%|
### 8.3 用户唯一标识强关联
device_id：使用 fingerprintjs 或本地生成 UUID 存入 localStorage，与 user_id 绑定。

跨端打通：通过微信登录/手机号绑定，将多设备 device_id 合并到同一 user_id。

## 九、安全与合规维度（免疫系统）
### 9.1 HTTPS 强制 + HSTS
所有环境强制 HTTPS，开启 HSTS（Strict-Transport-Security: max-age=31536000）。

### 9.2 接口签名机制（防重放攻击）
请求头：X-Timestamp（Unix 秒级时间戳）+ X-Nonce（随机字符串）+ X-Sign（HMAC-SHA256 签名）。

签名算法：Sign = HMAC-SHA256(secret, method + path + timestamp + nonce + body)

服务端校验：timestamp 与服务器时间差 ≤ 5 分钟；nonce 在 Redis 中缓存 5 分钟防重复。

### 9.3 敏感数据脱敏
日志脱敏：打印用户手机号时自动替换为 138****1234（在日志库层面用 logrus hook 实现）。

数据库加密：users.phone 字段使用 AES-256 加密存储（查询时解密，仅在必要时）。

### 9.4 隐私合规（GDPR / 个保法）
隐私弹窗：首次访问时弹窗，明确告知收集数据用途（改善报告准确性）。

选项：“同意并继续” / “仅使用必要数据”（后者限制 AI 功能）。

账户注销：在“设置”页提供 “永久注销账户” 按钮，点击后二次确认，7 天内可反悔（冻结），7 天后物理删除所有数据。

## 十、运营与增长裂变维度（燃料）
### 10.1 拉新裂变物料设计
分享卡片（人格徽章）：包含用户专属的“人格称呼”+ 情绪波形图 + 金句 + APP 品牌二维码。

运营后台配置项（预留管理界面）：

底部品牌文案（可改）

卡片主题色（可改）

二维码跳转链接（可改）

### 10.2 DeepLink 与 Universal Link（Web→APP 预留）
Web 端分享卡片二维码，扫码后进入 落地页（H5），检测是否安装 APP：

已安装：通过 URL Scheme 拉起 APP 并跳转至对应 Tab。

未安装：跳转至应用商店/小程序。

### 10.3 灰度发布与 Feature Flag
工具：自研简单开关（存储在 Redis），feature_flags Hash。

示例 Flag：

new_report_layout_v2（新版报告 UI）

daily_card_push（每日卡片推送）

ai_model_claude（切换模型）

控制台：后端提供 /admin/flags 接口，仅开发环境可访问。

## 十一、性能与监控维度（体检报告）
### 11.1 启动耗时拆分（Web 端首屏加载）
目标：LCP（Largest Contentful Paint）≤ 1.5s。

优化手段：

首屏只加载核心 HTML/CSS，JS 异步加载（defer）。

Tab1 数据在页面加载后通过 API 异步拉取，不阻塞渲染。

字体使用 font-display: swap。

### 11.2 崩溃率与错误监控
工具：Sentry（前端错误捕获）+ 后端日志（ELK 或自建）。

告警规则：5 分钟内错误率超过 5% 则发送企业微信/飞书机器人告警。

### 11.3 内存与性能
图片优化：

用户头像限制 ≤ 200KB（前端压缩）。

分享卡片图片生成后上传 OSS，不存储在内存中。

虚拟滚动：Tab3 日记列表若超过 50 条，采用虚拟滚动（react-window / vue-virtual-scroller）仅渲染可视区域。

### 11.4 包体积（Web 无需考虑，但为后续 APP 预留）
APP 包体积目标：≤ 100MB（IPA/APK）。

APP 资源压缩：图片使用 WebP，代码启用 Proguard 混淆（Android），资源按需下载。

## 十二、商业化变现维度（造血）
### 12.1 付费点埋入动线
时机（用户状态）	付费墙展示位置	付费点
Tab4 刷新次数用尽（极度渴望新洞察）	点击刷新时弹出半屏会员页面	订阅“深度校准会员”（¥9.9/月，无限刷新）
用户连续阅读报告超过 3 次（愉悦高峰）	报告底部出现“支持我们”横幅	一次性捐赠 / 买断制
用户尝试导出 PDF（未来功能）	导出按钮点击时提示“会员专享”	同上
### 12.2 价格锚点 UI
订阅页默认展示三档：

月度：¥9.9/月（标注“最灵活”）

年度：¥79/年（标注“节省39.8 ¥，最划算” —— 默认选中）

终身：¥299（标注“永久解锁，限量”）

按钮文案：不用“订阅”，用 “🔓 解锁无限洞察”。

## 十三：各Tab功能、设计与动线全景图（用户体验执行手册）
### 13.1 Tab1「我的前传」—— 人格拼图工坊
#### 13.1.1 页面心智模型
用户潜意识认知：这里是我的“人格仓库”。我不是在填表，而是在点亮一块块拼图。每一次点击都在让一个模糊的影子变得更清晰。

氛围基调：温和、有序、充满“收集欲”。进度环是核心视觉锚点，驱动用户“集齐所有拼图”。

#### 13.1.2 页面布局（从上到下）
|区域	|内容|	高度/占比
|顶部导航栏	|左：标题“📖 我的前传” / 右：❓帮助图标	|56px
|进度环区域	|圆形SVG进度环（显示 X/10）+ 下方激励文案（如“还差2个支柱即可生成初版手册”）	|180px
|支柱卡片网格	|10大支柱以 2列（移动端）/ 3列（桌面端） 瀑布流排列。每张卡片显示：图标 + 短名称 + 状态标识（“待探索”/“进行中”/“已锚定”）	|剩余高度，可滚动
|底部悬浮按钮	|“🔒 封存至人生底层”（仅当有未保存更改时亮起，否则置灰）|	56px（固定底部）
#### 13.1.3 核心操作动线
##### 动线A：首次进入

用户首次进入Tab1 → 直接展示支柱网格（12大支柱全部为”待探索”状态），无前置引导卡片。右上角 ❓ 图标可点击查看使用说明。

##### 动线B：日常完善一个支柱（点击卡片进入详情）

点击任意灰色/半透明卡片（如“肉身使用书”） → 右侧平滑滑入详情页（覆盖层，非新页面，保留底部Tab）。

详情页上半屏：3道基础题垂直排列（单选/滑动条）。选完即自动保存，选项高亮反馈（无提交按钮）。

详情页下半屏（折叠状态）——用于用户想要回答对应维度下额外的题目：显示一行引导文案 + 滑块（0-15）。用户拖动滑块 → 下方动态展开对应数量的气泡标签（从对应维度的题库随机抽取）。点击标签即选中/取消，实时保存。

用户下滑到底部，看到一个极小的可选备注框（默认折叠）：“还有想补充的吗？” → 点击展开输入。

点击左上角“返回”箭头 → 详情页向右滑出，主页面卡片进度同步更新（如“肉身使用书”从“待探索”变为“已锚定 3/3”）。

内容维度结构（10大生命支柱）
|序号|	支柱名称（UI展示名）|	核心采集内容（大白话方向）|
|1	|肉身使用书	|精力波点、睡眠密码、身体痛点（肩颈/肠胃/偏头痛）、饮食直觉|
|2	|情绪暗礁图	|高频情绪、炸毛开关、恢复能量的独家秘方、对噪音/寂静的敏感度|
|3	|思维操作系统	|决策习惯（理性/直觉）、学习方式（视觉/听觉/动手）、对新事物的本能态度|
|4	|社交光谱仪	|社交电量（充能/耗能）、亲密恐惧、在人群中的角色（领导者/观察者/调和者|
|5	|原生引力场	|父母教育模式、家乡文化与价值观的潜意识影响、家庭仪式感|
|6	|金钱与价值罗盘	|消费冲动类型、财富安全感等级、钱在你心中的象征意义（自由/安全/面子/工具）|
|7	|心流与战场	|工作心流状态、创造力爆发时刻、对职场权力文化的适应度、成就感来源|
|8	|闲暇与孤独美学	|独处回血方式、对“无聊”的耐受度、娱乐偏好（高刺激/低刺激）|
|9	|存在与意义花园	人|生剧本主线、谁是最重要的人、你想留下什么“痕迹”|
|10	|暗影与伤疤|	对老去的真实态度、最深的恐惧、从未示人的那面自我、身体政治敏感度|

##### 动线C：联动触发（从Tab2回流）

当用户在Tab2记录“被老板骂了”，后端AI判断关联支柱4（社交光谱）。用户下次打开Tab1时，支柱4卡片右上角出现小红点 + 气泡提示：“检测到相关记录，点我校准？” → 点击直接进入该支柱详情页，高亮对应标签（如“权威敏感”）。

#### 13.1.4 状态流转矩阵
|支柱状态	|卡片视觉	|进度环影响|
|未开始（3题均未答）	|灰色底 + 虚线边框，显示“待探索”	|不计入 X/10|
|进行中（1-2题已答）	|半透明底色 + 底部细进度条（33%/66%），显示“进行中”	|不计入 X/10|
|已锚定（3题全部答完）	|主色渐变底 + 右上角✅，显示“已锚定”	|计入 X/10|
|已锚定+深度标签（同上，且有选中标签）	|主色渐变底 + ✅ + 右下角小钻石💎图标	|计入 X/10（额外加权，AI报告更精准）|

#### 13.1.5 异常与边界处理
断网提交：基础题答案和深度标签先存入 localStorage，顶部出现黄条“离线模式，数据本地保存”，网络恢复后自动同步。

中途退出详情页：已选中的答案自动保存，无需用户手动点击“保存”。

### 13.2、Tab2「⚡ 定格此刻」—— 情绪采样中心
#### 13.2.1 页面心智模型
用户潜意识认知：这是一个“情绪快照亭”。走进去，15秒就能拿到一张“心灵拍立得”。不需要思考，只需要感受。

氛围基调：轻快、低阻力、彩色。大圆按钮、明亮色块，降低一切认知负荷。

#### 13.2.2 页面布局（从上到下）
|区域	|内容	|高度/占比
|顶部导航栏	|左：标题“⚡ 定格此刻” / 右：显示今日已定格次数（如“今日 3 次”）|	56px|
|三大分类入口	|3张巨大圆角卡片横向排列（或纵向堆叠，响应式）：
💭  此刻的情绪
🎯  此刻的行为
🌱  此刻的摄入/接触	|每张 80-100px|
|动态配置区	|根据所选分类，动态渲染下方内容（情绪轮/标签云/输入框）	|剩余高度，可滚动|
|底部主按钮	|“⚡ 定格此刻”（大圆角胶囊按钮，始终固定在底部）|	64px|
#### 13.2.3 核心操作动线
##### 动线A：完整定格流程（15秒极速版）

用户点击任一分类卡片（如“💭 心里泛起了什么”）→ 卡片放大选中态，下方平滑展开二级选项。

|二级选项：主情绪（入口）	|三级选项：次级质变（性质不同，非程度）	|四级选项：最精准的日常状态（选项供点击）|
|焦虑 (Anxiety)	|担忧、不安、恐慌、畏惧	|心慌意乱、患得患失、坐立难安、大难临头感、思虑过度、害怕失控、选择恐惧、分离焦虑|
|恐惧 (Fear)	|惊吓、忌惮、胆怯	|被吓一跳、毛骨悚然、不敢面对、怯场|
|平静 (Calmness)	|安宁、松弛、踏实、超然|	心如止水、放空、无忧无虑、舒坦、慵懒、不悲不喜、安稳入睡、呼吸顺畅|
|愤怒 (Anger)	|恼火、暴躁、憎恨、敌意	|被冒犯、不耐烦、咬牙切齿、想怼人、不公平感、被欺骗、气鼓鼓|
|厌恶 (Disgust)	|反感、恶心、鄙夷	|看不上、生理性不适、不想靠近、嫌弃、倒胃口|
|悲伤 (Sadness)	|忧郁、失落、哀痛、凄凉	|难过想哭、觉得空落落的、怀旧、绝望、孤单、被抛弃感、失望透顶|
|开心 (Joy)	|欣喜、兴奋、温暖、自豪	|满足、偷着乐、激动、甜蜜、被治愈、如释重负（正面版）、成就感、被宠爱|
|爱/信任 (Love/Trust)	|亲密、欣赏、依赖	|被理解、想靠近、温暖陪伴、感动、钦佩、安全感（人际版）|
|惊讶 (Surprise)	|惊喜、诧异、困惑、恍惚	|没想到、被惊艳到、一脸懵、不可思议、走神、发呆、解离感|

选择具体情绪后，下方自动露出：

一个“情绪体温计”滑动条（1-5级，默认3）。

一行来源按钮（👤我自己 / 👥他人 / 🌍外物），点击即选中。

一个极短输入框（占位：“一句话带过，选填”），不强制。

用户点击底部“⚡ 定格此刻” → 按钮进入 0.5秒加载态（变灰+微动效） → 随即弹出成功Toast。

定格成功反馈：Toast文案随机轮换（见维度五）。同时右上角“今日次数”+1，页面自动重置至初始状态（清空所有选中，回到三大分类卡片），方便用户连续记录下一条。

##### 动线B：叙事疗愈（入口一，深度模式）

在Tab2顶部，通过一个胶囊切换器（“闪电定格” / “叙事疗愈”）切换至入口一。

页面变为一个大输入框（占屏幕60%），占位符：“今天发生了什么？开心的、难过的、困惑的……我都想听。”

用户输入文字（≥20字），点击“🌿 递给我的树洞” → 进入Loading页（4个光点逐一亮起，见设计点2）。

加载完成 → 全屏模态展示分析报告（包含：心灵回响/镜中之镜/深层透视/与你同在/一剂行动微光）。

报告页面分为 上屏（固定） 与 下屏（操作区）。
上屏（报告内容，可滚动）：
🔖 心灵回响：[AI生成的意象短句]
第一层 · 镜中之镜：[100字以内的客观复述总结]
第二层 · 深层透视
专业棱镜：[心理学术语或现象描述]
人生回响：[联动Tab1的一句话，如果没有Tab1数据则显示“继续在Tab1点亮你的拼图，我能更懂你”]
第三层 · 与你同在：[高精度共情段落]
✨ 一剂行动微光：[一个微小行动建议]

下屏（操作区，固定在底部）：
左侧按钮（主要操作）：“📖 存入今日日记 & 人生基石”（绿色/主色）
右侧按钮（次要操作）：“💬 我还想再聊聊”（灰色/幽灵按钮）

点击“存入” → 保存 → 跳转回Tab2初始页。

点击“聊聊” → 报告页下半屏上推，进入聊天界面（类微信气泡），AI扮演“温柔的苏格拉底”。聊完后点击“🤝 沉淀为最终报告” → 重新生成报告 → 保存 → 跳转回。

#### 13.2.4 特殊状态与引导
首次进入Tab2（无任何记录）：三大分类卡片下方显示一行微动效引导：“点一下，把此刻交给我。”（10秒后自动淡出）。

连续记录第5次：在Toast弹出后，额外叠加一条横幅：“🎉 已积累5次定格！Tab4的说明书已解锁。”点击横幅 → 跳转Tab4。

### 13.3、Tab3「📅 时光日记」—— 情绪档案馆
#### 13.3.1 页面心智模型
用户潜意识认知：这是“我的情绪博物馆”。每一天是一个展品，走进去能看到当天的“情绪标本”。月度报告则是“策展回顾”。

氛围基调：沉静、整洁、有秩序感。留白充足，信息层级清晰。

#### 13.3.2 页面布局（从上到下）
|区域	|内容	|高度/占比
|顶部导航栏（三层结构）	|左：〈 年/月（返回按钮） / 中：“2026年7月”（可点击） / 右1：⇅（排序） / 右2：📊 周报（含小红点）	|56px
|日期卡片列表	|按月份聚合的日期卡片，从上到下排列（默认最新到最旧）。卡片样式见设计点4（含情绪色带、±分、标题钩子）。|	可滚动，虚拟滚动
|底部（可选）|	当月统计小标签：“本月共记录 12 个瞬间”	|极简，不占视觉重心

#### 13.3.3 核心操作动线
##### 动线A：日期切换与卡片排序

点击顶部中间“2026年7月” → 从底部上滑弹出滚轮选择器（年/月双列），选中后平滑关闭，列表刷新至对应月份。

点击右1 ⇅ → 卡片列表翻转动画，顺序从“新→旧”切换为“旧→新”。

##### 动线B：展开查看当日详情

点击任意有记录的日期卡片 → 卡片向下平滑拉伸（高度自适应），露出内部详情：

顶部显示微型“日内情绪波形图”（仅当≥3条定格时显示）。

记录条目列表:来自定格的记录，显示为灰色小气泡(例：14:30 · 😡 愤怒（4级） 老板骂·怒潮); 叙事记录，显示为带底色的卡片块(例：20:15 · 📖 暴雨前的蝉鸣)。

点击叙事记录块 → 从右侧滑入半屏弹窗，展示完整分析报告。

点击底部“✏️ 补记一笔” → 自动跳转至Tab2，并聚焦“闪电定格”。

再次点击卡片顶部 → 卡片向上收回至原始高度。

##### 动线C：生成周报与分享

点击右2 📊 周报 → 后端检查本周记录数。

若 < 3条 → 弹出轻提示：“再记录3个瞬间，周报即可破茧。”

若 ≥ 3条 → 全屏加载动画（2秒）→ 展示完整周报页（含情绪折线图+彩虹环+模式识别+下周指引）。

“周报 / 月报页面结构（滑动长图）
顶部（仪式感）：
一个大标题：“第 28 周 · 心灵航行报告”
副标题（AI生成的一句话总结）：“这一周，你像潮汐一样在‘焦灼’与‘释然’之间摆荡，但始终没停止向前。”
第一板块：情绪光谱图（核心视觉）
图表类型：平滑折线面积图（带渐变填充，正值区域填充暖色，负值区域填充冷色）。
横轴：周一至周日（或 1号-30号）。
纵轴：情绪综合分（-5 ~ +5）。
关键数据标注：在波峰/波谷处自动打点，显示“最高点：周六（+4，喜悦）”、“最低点：周三（-4，愤怒）”。
第二板块：本周/月情绪百宝箱（颗粒度还原）
展示一个 “情绪彩虹环”（环形图），统计本周各类情绪的占比（如：愤怒 20%、喜悦 15%、平静 30%）。
旁边列出 Top 3 高频关键词（AI从用户的“自定义锚点”和“心灵回响”中提取）：如 #工作压力、#亲密关系、#自我成长。
第三板块：与人生说明书（Tab1）的联动洞察（核心价值）
标题：“🔗 模式识别：你未言明的剧本”
内容示例：“结合你 Tab1 中的「情绪暗礁图」，你对‘权威否定’极其敏感。本周三的低谷（被领导质疑），再次触发了你的‘战逃反应’。但周六你主动运动（定格记录），这说明你已无意识地启动了自我调节。”
如果 Tab1 数据不足，则显示：“数据拼图尚缺几块，前往 Tab1 完善「心流与战场」，下周报告将解锁深层模式。”
第四板块：下周微光指引（Actionable Insight）
AI 只给 1 条 极其具体的建议，并附上一个极小的可点击按钮：“📌 记入明日待办”。
例如：“本周你周六情绪最高，建议下周将重要的社交活动安排在周六。”

周报页底部固定“📤 分享人格徽章”按钮。”

点击分享 → 生成抽象化分享卡片预览（不含原始数据）→ 用户确认 → 下载图片 / 复制文案。

“分享内容生成逻辑（不包含任何原始文本和具体数值）：
人格徽章：AI 根据本周数据生成一个酷炫的称号（如：“本周 · 自愈型孤勇者”）。
一句诗意文案：AI 将本周情绪趋势翻译成一句极具共鸣的短句（例如：“在七月的热浪里，我做了自己情绪的潮汐观测员。”）。
抽象可视化图：只保留 情绪折线图的轮廓（不显示纵轴刻度，仅保留走势形态）+ 彩虹环图（不显示百分比，仅保留配色）。
品牌露出：底部固定显示 APP 名称 + Slogan（如：“人生使用说明书 · 记录即疗愈”）。
引导下载二维码：附带一个极小的、带 App 下载链接的二维码（为拉新准备）。
用户操作流：
点击分享 → 预览卡片 → 选择“保存到相册”（生成为一张高清 9:16 图片）或“复制文案”。
交互细节（防社死）
分享前弹窗提醒：
“你即将分享的是一张「人格徽章卡」，它只展示你的成长态势，不包含任何具体日记内容。放心晒出真实的自己。”
（这大大降低了用户的分享阻力。）”

### 13.4、Tab4「📖 我的人生使用说明书」—— 终极启悟殿堂
#### 13.4.1 页面心智模型
用户潜意识认知：这里是“朝圣之地”。前面的所有付出，都是为了在这里看到“神谕”。它必须充满仪式感、不可亵渎。

氛围基调：庄严、纸质感、充满智慧。大量留白，衬线字体，米白背景。

#### 13.4.2 页面布局（根据解锁状态动态变化）
状态A：完全空白（0数据）

页面中央：精致拼图灰模插画（SVG）。

文案：“你的专属手册正在沉睡……”

按钮：“⚡ 去点亮第一块拼图”（跳转Tab1）。

状态B：数据不足（未达3支柱/5记录）

页面中央：破茧进度条（双轨进度：支柱 X/3，记录 X/5）。

文案精确提示：“还差 2个支柱 和 3条记录，初版说明书即可破茧。”

按钮置灰：“手册沉睡中……”

状态C：已解锁（可生成）

进度条填满，底部按钮变为彩色：“✨ 唤醒我的使用手册”（呼吸灯动效）。

状态D：已生成报告（报告展示页）

内容极长，可滚动。包含：

封面区：大号衬线字体显示“人格称呼”（如“克制中带着野心的晨间行者”），下方带版本号“V1.0”。
五章正文：每章标题用衬线体，正文用无衬线体。关键数据高亮（主色）。
AI密信区：仿羊皮纸底纹，斜体字，开头“亲爱的 [昵称]：”。

报告内容设计：
第一章：此刻的身份速写（封面人格）
内容：AI基于最新数据，凝练出不超过15个字的核心定位，并配一句解释。
示例：“克制中带着野心的晨间行者”。
解释：你习惯在清晨掌控世界（来自肉身使用书），但在事业上你渴望打破规则（来自心流与战场）。这种表里如一的张力，是你的核心燃料。
第二章：你的生命能量图谱（融合Tab1静态特质）
这里将10大支柱转化为 “能量开关” ，分为两类展示：
🔋 充能项（让你满血复活的特质）：如“独处时的创造力（闲暇美学）”、“深度阅读的沉浸感（思维系统）”。
🔋 耗能项（让你精疲力竭的雷区）：如“权威审视下的窒息感（社交光谱）”、“过度共情的耗竭感（原生引力场）”。
交互细节：每个开关点击可展开AI给出的具体证据链（例如：“7月14日你记录‘被老板否定后瘫软’，印证了权威耗能。”）
第三章：近期运行日志分析（融合Tab2动态数据 + Tab3周报趋势）
板块A：情绪底层算法。
展示一张缩微版的情绪走势图（过去30天），并框出最高峰和最低谷。
AI 洞察：“过去一个月，你的情绪低谷频繁出现在周一/周二，高峰出现在周六。你的身体在告诉你：工作日的‘面具’比想象中更沉重。”
板块B：未言明的剧本（盲点挖掘）——这是“哇塞”时刻的核心！
AI 对比你的静态特质（Tab1）和动态行为（Tab2），找出你自己没发现的矛盾点。
示例：“你在Tab1中说自己是‘回避型依恋’，但记录显示你在深夜频繁因亲密关系焦虑。这说明你并非不渴望连接，而是害怕失控。你比你以为的更勇敢，也比你以为的更孤独。”
第四章：你的维护与保养指南（导师建议）
将生硬的“建议”转化为“使用注意事项”。
分条目给出 3条 高度个性化的操作指南（颗粒度极细）：
“燃料补给：鉴于你的精力巅峰在早7-9点，建议将最难的工作安排在这个时段，下午2-4点安排机械性重复劳动。”
“故障预警：当感到‘肩膀僵硬’时（你身体的诚实信号），意味着你正在压抑愤怒，请立即离开座位做5次深呼吸。”
“升级路线：你读过的书集中在文学类，但欠缺心理学底层架构。推荐你下一本阅读：《也许你该找个人聊聊》。”
第五章：版本更新日志（V1.0.1）
在报告最底部，用极简的时间线记录每一次“刷新”带来的变化。
“V1.0.1（2026-07-14）：检测到新增5条‘工作焦虑’定格，优化了耗能项权重。”
“V1.0.0（2026-07-01）：基于3大支柱+5条记录，初版手册生成。”
第六章（终章）：来自AI的一封密信（情感升华）
一个展开的羊皮纸风格区域，开头是 “亲爱的 [用户昵称]：”
AI根据所有数据，写一段300字左右的充满文学性和哲思的鼓励信。不是鸡汤，而是基于事实的终极接纳。
结尾固定句式：“这本说明书不是要定义你，而是要提醒你：你有权成为任何版本。我一直在看。”

底部固定工具栏：

左：“📤 导出PDF”（未来付费点，现显示“即将上线”）

右：“🔄 校准指南针”（显示剩余次数，如“剩余2次”）。

#### 13.4.3 核心操作动线
动线A：首次唤醒报告

用户点击“✨ 唤醒我的使用手册” → 全屏加载动画页（书写的墨迹动画，5秒）。

加载完成后，页面从顶部渐入，直接展示完整报告，滚动条自动回到顶部。

用户滚动阅读，在“盲点挖掘”章节产生“哇塞”时刻。

阅读完毕后，用户点击底部“校准”或退出。

动线B：刷新报告（校准）

用户点击“🔄 校准指南针” → 后端检查 refresh_quota。

若 quota > 0：再次进入加载动画（2-3秒，因为增量生成更快）→ 新报告覆盖旧报告，页面顶部闪动Toast：“报告已进化。”

若 quota = 0：底部弹出半屏付费墙：“本月洞察次数已用尽。开通无限校准，持续追踪你的成长。”（含订阅按钮）。

刷新后，报告“版本日志”章节自动追加一条新记录：“V1.1（2026-07-20）：基于新增3条记录，优化了能量图谱权重。”

#### 13.4.4 页面切换与状态保持
从Tab4切到其他Tab再返回：不重新生成报告，直接显示缓存的最新报告（life_manual 表中 is_current=true 的记录）。无加载等待，秒开。

附：Tab间跳转关系与全局动线汇总
起点	终点	触发条件	动效/反馈	数据联动
Tab1（首次建档后）	Tab2	引导浮层点击“去记录”	浮层淡出 → Tab底部切换	无
Tab2（完成定格）	Tab1	进度达标自动触发横幅	横幅点击跳转	更新 pillar_completed 状态
Tab2（定格中）	Tab3	直接底部切换	即时切换	日记列表自动刷新
Tab3（展开日记）	Tab2	点击“补记一笔”	底部Tab自动切换至Tab2	无
Tab3（周报分享）	外部（朋友圈）	点击分享	生成本地图片	记录 share_card_created 埋点
Tab4（点击唤醒）	自身（报告页）	数据达标后点击	全屏加载动画	写入 life_manual 表
任意Tab	Tab4	数据达标首次	全局横幅弹出（在任意页面顶部滑入）	点亮Tab4图标

## 附录：AI Prompt 模板

A1. 入口一叙事分析（Tab2 入口一）
markdown
## Role
你是一位兼具深厚心理学底蕴与文学素养的人生观察员。你的语言温暖、精准、不鸡汤。

## Task
用户输入了一段关于自己经历、情绪或困惑的文字。请按照以下严格的 JSON 格式输出分析结果。

## Output Format (JSON)
{
  "title": "一个极具画面感、不超过10个字的隐喻短句",
  "mirror": "用简洁、中性的语言复述用户事件的核心，不超过80字",
  "psychology": "匹配一个专业的心理学术语并给出通俗解释",
  "life_link": "基于用户输入内容，猜测用户可能对应的Tab1支柱标签并自然表述。若无法推测，则固定回复：'完善人生拼图后我能看得更清。'",
  "empathy": "针对用户原文中的具体矛盾点进行深度共情",
  "action": "只输出1个具体的、可触摸的微小行动"
}
## Rules
严禁使用'你应该'、'你必须'等命令式说教口吻。输出必须为纯JSON。
A2. Tab4 报告生成
markdown
## Role
你是一位具有30年临床经验的整合心理学派导师，同时也是一位擅长人物特写的作家。

## Input Data
1. User_Profile (Tab1的10大支柱基础答案)
2. Recent_Records (Tab2近30天的情绪/行为定格 + 叙事记录摘要)
3. Weekly_Trend (Tab3的情绪波动摘要)
4. Last_Report (如果是刷新，提供上一版本全文)

## Task
生成一份名为“人生使用说明书”的深度报告。严格按以下五章结构输出（Markdown）。
- 第一章：身份速写（一句话封面+解释）
- 第二章：能量图谱（充能项与耗能项，各3条，带证据链）
- 第三章：运行日志（情绪算法分析 + 盲点挖掘，必须指出用户未察觉的矛盾）
- 第四章：维护指南（3条极其具体的可操作建议）
- 第五章：版本日志
最后，单独写一封300字的“AI密信”。

## Critical Rules
严禁使用“可能”、“或许”、“大概”等模糊词汇。要笃定！盲点挖掘必须基于数据矛盾。输出纯文本Markdown。
附：给 Claude Code 的最终启动指令
你现在已经完整理解了“人生使用说明书”这个产品的全部维度。

请按照以下优先级生成 Web 端 MVP 代码：

数据库建表语句（PostgreSQL + Redis 配置）

后端 API 骨架（Node.js + Express 或 Python + FastAPI，按你选择的技术栈）

前端页面（React / Vue，按你选择的技术栈）

优先实现：价值主张页 → 3题建档 → Tab1 支柱展示 → Tab2 定格记录 → 进度触发 → Tab4 报告生成（初期用 Mock 数据）

所有代码遵循本文档定义的设计令牌、API 规范、埋点规范、无障碍规范。

在代码关键位置添加注释，标注出为未来 APP/小程序预留的接口和扩展点。